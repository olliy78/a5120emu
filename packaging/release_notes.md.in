# K1520emu @TAG@

Hardwarenahe Emulation des **Robotron A5120**, eines Z80-Bürocomputers des VEB Robotron
Dresden (DDR, 1982). Nachgebildet wird nicht das Betriebssystem, sondern der **K1520-Bus
mit seinen Steckkarten** — es gibt keine BIOS-Traps: Boot-ROM, BIOS und Betriebssystem
laufen als echter Z80-Code. Deshalb bootet die Maschine mehrere Original-Betriebssysteme
von Original-Disketten: **CP/A**, **SCPX** und **UDOS**.

Beschreibung, Aufbau und Entwicklungsstand: **[Projektseite mit README](https://github.com/olliy78/a5120emu#readme)**

<!-- Der folgende Abschnitt gehoert zur JEWEILIGEN Version und wird vor jedem
     Tag neu geschrieben.  Er steht hier und nicht in einer CHANGELOG-Datei,
     weil die Release-Seite der einzige Ort ist, an dem ein Downloader ihn
     liest.  Alles darunter ist versionsunabhaengig. -->

---

## Was ist neu in @VERSION@

Der Schwerpunkt dieser Vorabversion liegt auf **echten Disketten** und auf dem, was man
mit ihnen anstellen kann, wenn sie beschädigt oder von einem fremden Rechner sind.

- **Echte Diskette am Greaseweazle-Adapter.** Ein angeschlossenes 5,25″- oder 8″-Laufwerk
  wird **spurweise nach Bedarf** gelesen und beschrieben — der Zwischenschritt „ganze
  Diskette in eine Datei" entfällt, eine Diskette ist in Sekunden statt Minuten offen.
  Beide Programme können es, an echter Hardware nachgewiesen. Geschrieben gilt dabei erst
  nach dem **Zurücklesen**, sodass Schadstellen auffallen statt unbemerkt zu bleiben.
- **Dateisystemprüfung und Reparatur im k1520DiskTool** (`fsck`): Eine Diskette wird beim
  Öffnen geprüft, Befunde lassen sich einzeln reparieren, und **gelöschte Dateien** kann
  man wiederfinden und herausholen — bei CP/M, ZDOS und NDOS. Vorausgewählt ist dabei nur,
  was keine Daten verwirft.
- **Drei weitere Dateisysteme gelesen und geschrieben:** **UDOS1715/NDOS** (PC 1715),
  **SCP1700/CP/M-86** (A7100) und die NDOS-Ausprägung der **P8000**.
- **Fremd beschriebene Disketten** — an einem anderen K1520-Rechner geschriebene UDOS-
  Disketten waren bisher unlesbar; Sync-Gruppe, CRC-Sitte und Spurnaht sind nachgezogen.
- **Diskeditor**: die Diskette Sektor für Sektor ansehen und ändern, Spuren einfügen und
  löschen, den UDOS-Anhang hinter der Prüfsumme bearbeiten.
- **Bootfähige Disketten anlegen** — CP/A, SCPX und UDOS, aus einer Leerdiskette heraus.
- **Nachgebildete K7637-Tastatur** im Emulator: das Tastenfeld zeigt mit, jede PC-Taste
  spricht die Taste an ihrer Stelle an.
- **Überarbeitete Oberflächen** in beiden Programmen: einrichtbare Symbolleiste,
  Zustandszeile statt Zähler, mitgeliefertes Handbuch, erkanntes Diskettenformat — und ein
  Menü *Werkzeuge*, über das beide Programme einander und eine Konsole für Debugger und
  Kommandozeile starten.
- **Der Debugger `k1520dbg` liegt jetzt bei** (Linux und Windows), mit Konsolenmodus und
  eigenem Handbuch.

Alles im Einzelnen: der Änderungsverlauf am Ende dieser Seite.

---

## Welche Datei brauche ich?

| Datei | System | Wofür |
|-------|--------|-------|
| **`K1520emu-@VERSION@-win-x64-setup.exe`** | Windows 10/11, 64 Bit | **Doppelklicken, dem Assistenten folgen.** Er fragt, ob für alle Benutzer (`C:\Program Files`, braucht Administratorrechte) oder nur für Sie (`%LOCALAPPDATA%\Programs`), dann nach dem Diskettenordner — und richtet alles ein. Legt Startmenü-Einträge an und lässt sich über *Einstellungen → Apps* wieder entfernen. |
| `k1520emu-@TAG@-linux-x86_64.tar.gz` | Linux, x86-64 | Auspacken und `./install.sh` aufrufen. |
| `*.sha256` | | Prüfsummen. Kontrolle: `sha256sum -c <datei>.sha256` bzw. unter Windows `Get-FileHash <datei>`. |

Beide Systeme installieren **benutzerlokal, ohne Administratorrechte**.

## Installieren

```sh
# Linux
tar xzf k1520emu-@TAG@-linux-x86_64.tar.gz
cd k1520emu-@TAG@-linux-x86_64
./install.sh                     # fragt nach Ziel und Diskettenordner
```

Unter Windows macht das der Assistent. Gefragt wird beide Male zweimal: nach dem
**Installationsverzeichnis** und nach dem Ordner für die
**Arbeitsdisketten** (Vorschlag `<Dokumente>/K1520emu`). Der zweite ist bewusst getrennt —
dorthin schreibt der Emulator Änderungen an einer Diskette zurück, und ein Update fasst
ihn nie an.

## Was beim ersten Mal passiert

Das Setup ist ~3 MB, die Installation lädt danach **einmalig ~120 MB** nach: einen eigenen
Python 3.12 und Qt, beides **innerhalb der Installation**. Das braucht eine
Internetverbindung und ein paar Minuten — der Assistent sagt es vorher an und zeigt,
wo er gerade steht. Ein vorhandenes Python auf dem Rechner wird nicht angefasst, nichts
registriert; beim Entfernen bleibt nichts zurück. Danach startet der Emulator ohne Netz, und es sind
rund 150 MB (Linux) bzw. 116 MB (Windows) belegt. Systempython und Systempakete werden
nicht angefasst.

Sechs Beispieldisketten sind dabei — je eine startfähige für CP/A, SCPX und UDOS sowie zwei
Combo-Disketten mit Fremdlaufwerkstypen. Sie landen beim ersten Start im Diskettenordner.

## Gut zu wissen

- **Windows: SmartScreen warnt** beim ersten Start, weil die Datei nicht signiert ist
  („Weitere Informationen" → „Trotzdem ausführen"). Ein Code-Signing-Zertifikat kostet
  laufend Geld; das ist bewusst noch nicht angeschafft.
- **Deinstallieren:** Windows über *Einstellungen → Apps* oder den Startmenü-Eintrag,
  Linux mit `./install.sh --uninstall`. Die Arbeitsdisketten bleiben dabei stehen —
  sie liegen außerhalb der Installation, im Diskettenordner.
- **Mitgeliefert ist außerdem das `k1520DiskTool`** — damit holt man Dateien von CP/A-,
  SCPX- und UDOS-Disketten heraus und schreibt sie zurück (`.img`, `.hfe`, `.dmk`).
- **Und der Debugger `k1520dbg`** — ein Werkzeug für die Konsole, mit dem sich fremde
  Programme Schritt für Schritt untersuchen lassen (Haltepunkte, Register, Speicher,
  Rückwärtslaufen). Er hat bewusst kein eigenes Symbol; unter Windows führt der
  Startmenü-Eintrag *„K1520-Werkzeuge (Eingabeaufforderung)"* hin, das Handbuch liegt
  in der Installation unter `share/doc/handbuch_k1520dbg.md`.
- **Echte Disketten**: mit einem
  [Greaseweazle](https://github.com/keirf/greaseweazle)-Adapter lesen und beschreiben
  beide Programme ein angeschlossenes 5,25″- oder 8″-Laufwerk direkt. Die Anbindung
  richtet der Installer mit ein — ohne Adapter stört sie nicht.

## Geht etwas schief?

Die Installation protokolliert jeden Schritt: unter Windows in `bootstrap.log` im
Installationsverzeichnis, unter Linux in der Ausgabe von `install.sh`. Bricht der
Assistent vorzeitig ab, ist noch nichts installiert — dann steht der Grund dort. Wo der Emulator
seine Dateien sucht, beantwortet `python3 app/main.py --paths` in der Installation.

---

**Alle Änderungen:** https://github.com/olliy78/a5120emu/commits/@TAG@
